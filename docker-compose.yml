version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: gp_financas_backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env